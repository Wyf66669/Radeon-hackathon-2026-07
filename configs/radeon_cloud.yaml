# Optional overlay loaded automatically when /workspace/persistence exists.
# Relative paths still resolve via PLA_DATA_ROOT / detect_data_root().
# Keep sample_docs in the git tree; only runtime state is cloud-persistent.

llm:
  backend: "local_transformers"
  device: "cuda"
  dtype: "float16"
