[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "privatelocalagent"
version = "0.1.0"
description = "PrivateLocalAgent — Track 2 local private agent"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["src*"]
